upstream frontend_app {
    server frontend:4173;
}

upstream backend_app {
    server backend:8000;
}

server {
    listen 80;
    server_name www.potencore.ru 45.151.30.103;
    return 301 http://potencore.ru$request_uri;
}

server {
    listen 80;
    server_name potencore.ru 45.151.30.103;

    client_max_body_size 10m;

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    location /api/ {
        proxy_pass http://backend_app;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        proxy_pass http://frontend_app;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Для production с TLS обычно добавляется отдельный server block с 443 и сертификатами:
# server {
#     listen 443 ssl http2;
#     server_name potencore.ru www.potencore.ru 45.151.30.103;
#     ssl_certificate /etc/letsencrypt/live/potencore.ru/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/potencore.ru/privkey.pem;
# }
